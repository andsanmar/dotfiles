if [[ $1 == "move" ]]; then
    newContainer=$(($(i3-msg -t get_workspaces | tr , '\n' | grep '"num":' | cut -d : -f 2 | sort -rn | head -1) + 1))
    i3-msg move container to workspace $newContainer && i3 workspace $newContainer
else
    i3-msg workspace $(($(i3-msg -t get_workspaces | tr , '\n' | grep '"num":' | cut -d : -f 2 | sort -rn | head -1) + 1))
fi
